/**
 * This is the main part of the project
 */
public class Main {
    /**
     * It calls the controller for the code execution of an MVC structure.
     * @param args of main if needed
     */
    public static void main(String[] args) {
        Controller c = new Controller();
        c.execute();
    }
}
